name: Prerelease to NuGet

on:
  pull_request:
    types: [closed]
    branches:
      - main

  workflow_dispatch:
    inputs:
      semver-build-prefix:
        description: "Prerelease prefix (preview, alpha, beta, rc)"
        type: choice
        options:
          - preview
          - alpha
          - beta
          - rc
        default: preview

      reason:
        description: "Reason for manual pre-release"
        required: true
        type: string

      force-publish:
        description: "Force publish even if already tagged"
        type: boolean
        default: false

jobs:
  log-trigger:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Log manual trigger reason
        run: |
          echo "::notice::Manual release triggered by ${{ github.actor }}"
          echo "::notice::Reason: ${{ inputs.reason }}"

  prerelease:
    name: Prerelease
    if: github.event_name == 'workflow_dispatch' || (github.event.pull_request.merged == true)
    uses: vmelamed/vm2.DevOps/.github/workflows/_prerelease.yaml@37864b56b22723eaa8e6305d0dd0426017fca77f # replace with main branch once stable
    permissions:
      contents: write
      packages: write # Required for GitHub Packages
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_GITHUB_KEY }}
    with:
      package-project: "./vm2.TestUtilities.slnx"
      dotnet-version: ${{ vars.DOTNET_VERSION || '10.0.x' }}
      force-publish: ${{ inputs.force-publish || false }}
      semver-build-prefix: ${{ inputs.semver-build-prefix || 'preview' }}
      save_package_artifacts: true
      nuget-server: github # Publish to GitHub Packages
